# Legacy stack matching the original MFE vLLM 0.9.x migration scaffold.
# Use only when the Ascend image/environment was built for this stack.
torch==2.5.1
torch-npu==2.5.1
vllm==0.9.0
vllm-ascend==0.9.0rc2
transformers==4.53.0
